# İlerleme Günlüğü — 2026-06-10

## Bugün Tamamlananlar

### Sanal Laboratuvar Ortamı (FR-01)

- **SimVM-Attacker** oluşturuldu (SimVM-Normal'den full clone)
- Her iki VM'e statik IP atandı:
  - SimVM-Normal: `172.19.237.100/20`
  - SimVM-Attacker: `172.19.237.200/20`
- Klon makinede ağ arayüzü MAC tabanlı `match` ile sabitlendi
- Çakışan ikinci netplan dosyası (`00-installer-config.yaml`) temizlendi

### SimVM-Normal — Hedef Servisler

- Apache HTTP (port 80)
- OpenSSH (port 22)
- vsftpd FTP (port 21)
- SmartHome AI Assistant API — FastAPI (port 8080)

### SimVM-Attacker — Saldırı Araçları

- nmap, hping3, hydra, nikto, slowloris
- build-essential, gcc, g++

## Karşılaşılan ve Çözülen Sorunlar

- Eduroam DNS filtrelemesi → gateway DNS ile çözüldü (bkz. R-03)
- PEP 668 → `--break-system-packages` (saldırgan için kabul edildi, bkz. R-04)
- Klon arayüz uyumsuzluğu → MAC match ile çözüldü

## Mevcut Mimari Durum

```
[SimVM-Normal]  ✅      [SimVM-Attacker]  ✅
 172.19.237.100          172.19.237.200
 4 servis çalışıyor       5 saldırı aracı hazır
      │                        │
      └──────────┬─────────────┘
                 │  (henüz: HIDS yok)
                 ▼
        [Host / Raspberry Pi]  🔧 sonraki adım
```

## Sonraki Adımlar

1. **Ağ bağlantı testi** — Attacker → Normal ping ve port erişimi doğrulaması
2. **Saldırı senaryoları** — nmap scan, hping3 SYN flood, hydra SSH brute force, slowloris betikleri
3. **HIDS paket yakalama** — Host/Pi üzerinde Scapy ile canlı trafik dinleme
4. **XGBoost entegrasyonu** — Benchmark'ta eğitilen modelin canlı tespite bağlanması
5. **Dashboard bağlantısı** — Gerçek tespit sonuçlarının web arayüzüne akıtılması
